{% comment %}
  Default page template (fallback for pages without custom templates)
{% endcomment %}

<section class="page-section">
  <h1 class="section-title">{{ page.title }}</h1>
  
  <div class="section-content">
    {{ page.content }}
  </div>
</section>

